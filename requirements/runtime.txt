# Runtime dependencies: should match install_requires in setup.py.  Specifying them here in a
# requirements file makes it easier to build the wheelhouse.

appdirs
BlazeUtils
blinker
Click>=3.0
Flask>=0.10.1
Flask-SQLAlchemy
pathlib
six
